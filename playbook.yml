---
- name: Update APT package index
  hosts: all
  become: yes
  tasks:
    - name: Update APT package index
      apt:
        update_cache: yes
        cache_valid_time: 3600  # Update the cache if it's older than 1 hour


